{
  "name": "student-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "PERN Stack + Python AI Student Management System",
  "scripts": {
    "dev": "concurrently -n FE,BE,AI -c cyan,yellow,magenta \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:python\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "dev:python": "node start-python.js",
    "install:all": "npm --prefix frontend install && npm --prefix backend install",
    "build:frontend": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
